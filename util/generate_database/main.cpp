#include <vector>

#include <opencv2/core.hpp>
#include <opencv2/imgproc.hpp>
#include <caffe/caffe.hpp>
#include <glog/logging.h>
#include <glog/stl_logging.h>
#include <yaml-cpp/yaml.h>

using std::string;
using std::vector;


void generateDatabase(string rootFolder, vector<string> classes) {
    





}

int main(int argc, char** argv) {
    /*
    FLAGS_alsologtostderr = 1;
    google::InitGoogleLogging(argv[0]);
    google::InstallFailureSignalHandler();

    YAML::Node node = YAML::LoadFile(string(argv[1]));

    string rootFolder = node["RootFolder"].as<string>();
    vector<string> classes = node["Classes"].as<vector<string>>();

    generateDatabase(rootFolder, classes);

    */
    return 0;
}
